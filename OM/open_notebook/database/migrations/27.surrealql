-- Migration 27: дефолтное пространство «Админ-панель» (kind=default, slug=admin).
-- Неудаляемое системное пространство со своей отдельной страницей (структура —
-- пока каркас, важно наличие пространства в сайдбаре). order=6 — после видимых
-- дефолтных (Архив order=5 скрыт), поэтому идёт последним в списке.

INSERT INTO space [ { name: "Админ-панель", kind: "default", slug: "admin", icon: "LayoutDashboard", description: "Администрирование: пространства, доступы и системные настройки.", removable: false, hidden: false, order: 6 } ];
